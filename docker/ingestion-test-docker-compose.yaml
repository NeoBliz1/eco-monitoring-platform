include:
  - ./compose/kafka.yaml
  - ./compose/schema-registry.yaml
  - ./compose/consul.yaml
  - ./compose/vector/vector-sidecar.yaml

volumes:
  vector_disk_buffer:
    driver: local

secrets:
  kafka_server_jaas:
    file: secrets/scram/kafka_server_jaas.conf
  schema_registry_jaas:
    file: secrets/scram/schema_registry_jaas.conf

networks:
  eco-net:
    driver: bridge